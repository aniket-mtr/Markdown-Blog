<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= article.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: #e7dff9;
            font-family: Arial, sans-serif;
        }
        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
        h1 {
            text-align: center;
            margin-bottom: 20px;
        }
        .article-meta {
            text-align: center;
            margin-bottom: 20px;
            color: #6c757d; /* Bootstrap's text-muted color */
        }
        .article-content {
            margin-bottom: 30px;
            text-align: left;
        }
        .buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 20px;
        }
        .btn-custom {
            padding: 10px 20px;
            font-size: 16px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1><%= article.title %></h1>
        <div class="article-meta">
            <%= article.createDate.toLocaleDateString() %>
        </div>
        <div class="buttons">
            <a href="/" class="btn btn-danger btn-custom">All Articles</a>
            <a href="/articles/edit/<%= article.slug %>" class="btn btn-info btn-custom">Edit</a>
        </div>
        <div class="article-content">
            <%- article.sanitizedHTML %>
        </div>
    </div>
</body>
</html>
