<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= article.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body{
            background-color: #e7dff9;
        }
        .center{
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            box-shadow: sienna;
        }
        .buttons{
            display: flex;
            flex-direction: row;
            gap: 20px;
        }

    </style>
</head>
<body>
    <div class="center">
        <h1 class="mb-1"><%= article.title %></h1>
        <div class="text-muted mb-2"><%= article.createDate.toLocaleDateString() %></div>
        <div class="buttons">
            <a href="/" class="btn btn-danger mb-3">All Articles</a>
            <a href="/articles/edit/<%= article.slug %>" class="btn btn-info mb-3">Edit</a>
        </div>
        <div>
         <%- article.sanitizedHTML %>
        </div>
    </div>
</body>
</html>

